import { actionTypes } from './actions'
import nprogress from 'nprogress'
import { message, Modal } from 'antd'

/**
 * Initial state a giphy
 *
 */
const initialState = {
 
}

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
